name: 📰 Tistory RSS → 기자/ (helena-piano)

# 중앙 재사용 워크플로 호출 — 로직은 helena_phone/.github/workflows/tistory-sync.yml
on:
  schedule:
    - cron: '7 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    uses: helena751107/helena_phone/.github/workflows/tistory-sync.yml@main
    with:
      blog: helena-piano
